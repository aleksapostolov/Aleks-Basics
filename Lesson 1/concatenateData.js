function readName(input){
    let firstName = "Aleks"
    let secondName = "Apostolov"
    let age = 32
    let str = firstName + " " + secondName + " " + age;
    let str2 = `${firstName} ${secondName} ${age}` // new syntaxis to be used
    console.log(str);
    console.log(str2);
}
readName ();