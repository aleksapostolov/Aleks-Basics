function stringOfSymbols(input){
    let text = input[0];

 
    for(let i = 0; i < text.length; i++){
        console.log(text.charAt(i))
}
}
stringOfSymbols(["softuni"]);
stringOfSymbols(["ice cream"]);